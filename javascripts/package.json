{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "golive": "npm run build && npx shipit client deploy",
    "deploy": "npx shipit client deploy"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "markdown-it": "^13.0.1"
  }
}
